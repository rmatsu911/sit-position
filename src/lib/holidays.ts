/**
 * 工程表の休日表示に使う祝日リスト（`yyyy-MM-dd`）。
 *
 * 注意: これは表示装飾のためのデータであり、祝日マスタは未整備。
 * 実運用では組織の休日設定（会社カレンダー）をDB化し、ここを置き換える想定。
 * 未登録の年は祝日が着色されないだけで、土日の判定はカレンダーから計算される。
 */
export const JP_HOLIDAYS: readonly string[] = [
  '2026-07-20', // 海の日
]
